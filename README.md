# Anno XML Tool

A utility tool for Anno 117 modding to analyze and export assets, templates, and texts.

## Features
- Recursive search for XML files.
- Syntax highlighting for XML.

## Development
The tool is based on **Python 3.11** and **PyQt6**.
Build instructions can be found in `tasks.json`.